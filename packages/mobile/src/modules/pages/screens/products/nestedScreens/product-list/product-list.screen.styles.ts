import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	inputName: {
		borderWidth: 1,
		borderColor: '#ccc',
		padding: 10,
		borderRadius: 8,
	},

	sortBox: { flexDirection: 'row', marginTop: 12 },

	listContainer: {
		flexGrow: 1,
		width: '100%',
	},
	loadingIndicator: {
		marginVertical: 16,
	},
});

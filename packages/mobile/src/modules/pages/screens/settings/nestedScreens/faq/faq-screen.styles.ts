import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	list: {
		padding: 16,
	},
	answerText: {
		paddingHorizontal: 16,
		paddingBottom: 16,
		color: '#444',
		lineHeight: 20,
	},
});
